defaults:
  - model: default
  - mlflow: evaluation
  - data: evaluation
  - misc
  - _self_

batch_size: 128
checkpoint_step: ???

# seems to be working
# now we add configurations for evaluation
model:
  adapters:
    finetuning:
      pretrained_path: ???
      with_head: True

# now we add configurations for evaluation
