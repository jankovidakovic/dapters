defaults:
  - finetuning

model:
  adapters:
    pretraining:
      pretrained_path: ???
      with_head: False
    finetuning:
      config: pfeiffer
      reduction_factor: ???
      with_head: True
    train:
      - finetuning